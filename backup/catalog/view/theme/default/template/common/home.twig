{{ header }}
<div id="common-home" class="container-fluid p0">
    <div class="row m0">{{ column_left }}
        {% if column_left and column_right %}
        {% set class = 'col-sm-6' %}
        {% elseif column_left or column_right %}
        {% set class = 'col-sm-9' %}
        {% else %}
        {% set class = 'col-sm-12 p0' %}
        {% endif %}
        <div id="content" class="{{ class }}">
            {{ content_top }}
        </div>
        {{ column_right }}
    </div>
</div>
{{ content_bottom }}
{{ footer }}